// @ts-check
import eslintConfig from '@antfu/eslint-config'

export default eslintConfig(
  {
    // react: true,
    ignores: [
      // eslint ignore globs here
    ],
  },
  {
    rules: {
      // overrides
    },
  },
)
